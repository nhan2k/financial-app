import { ButtonClassPipe } from './button-class.pipe';

describe('ButtonClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonClassPipe();
    expect(pipe).toBeTruthy();
  });
});
